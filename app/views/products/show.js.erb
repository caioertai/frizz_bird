product = document.getElementById('product')
results = product.querySelector('.results-content')
results.parentElement.removeAttribute('style')
results.innerHTML = "<%= j render 'show' %>"
document.smoothScrollTo(product);
document.setMainLoading(false);